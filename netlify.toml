[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js runtime plugin handles all routing — no manual redirects needed
[[plugins]]
  package = "@netlify/plugin-nextjs"
